version: '3.8'

services:
  web:
    build: .
    container_name: my-website
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html  # Для Nginx
      # - ./:/var/www/html        # Для Apache